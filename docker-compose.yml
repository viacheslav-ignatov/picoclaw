services:
  picoclaw-gateway:
    build: .
    restart: unless-stopped
    volumes:
      - picoclaw-workspace:/home/picoclaw/.picoclaw/workspace
    env_file:
      - .env
    networks:
      - dokploy-network

volumes:
  picoclaw-workspace:

networks:
  dokploy-network:
    external: true
